//pensez a mettre le header

#include "vector.h"

t_vector	*vct_cdup(t_vector *vct, char c)
{
	t_vector *new_vct;*
	size_t	i;

	i = 0;
	vct = vct_new();
	if (vct == NULL)
		return (NULL);
	while (i < 
	
	
}
